info:
  version: 1.0.0
  title: backend
  description: MobileStore APIs

basePath: /

auth:
  path: /auth

  register:
    method: post
    path: /auth/signup
    params:
      username: string
      email: string
      image: string
      email: string
      country: string
      phone: sitrng
      password: string
      roles: array
      
  login:
    path: /auth/signin
    method: post
    params:
      email: string
      password: string
    response:
      status: 200
      data:
        token: string
        user: object user

products:
  path: /products
  create a phone:
    path: /products
    method: post
    params: 
      name: string
      image: string
      category: string
      description: string
      guarantee: number
      color: string
      price: number
      available: string

  get products by category:
    path: /category/:category
    method: get
    response:
      status: 200
      data: products (list products of category)

  get products by id:
    path: /products/:id
    method: get
    response:
      status: 200
      data: products (object phone)

  get all phone:
    path: /products
    method: get
    response:
      status: 200
      data: products (list products)

  delete products by id:
    path: /products/:id
    method: delete
      response:
      status: 200

  delete all phone:
    path: /products
    method: delete
      response:
      status: 200


users:
  path: /users
  get users:
    path: /users
    method: get
    response:
      status: 200
      data: users (list users)

  get user by id:
    path: /:id
    method: get
    response:
      status: 200
      data: user (object user)

  create new user:
    path: /
    method: post
    params:
      username: string
      email: string
      image: string
      email: string
      country: string
      phone: sitrng
      password: string
      roles: array
    response:
      status: 201
      data: user

  update user:
    path: /:id
    method: put
    params:
      username: string
      email: string
      image: string
      email: string
      country: string
      phone: sitrng
      password: string
      roles: array
    response:
      status: 200

  delete user:
    path: /:id
    method: delete
    response:
      status: 200
